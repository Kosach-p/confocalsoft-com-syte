# Статичная версия

Статичная версия предназначена для серверных сред.
Она не имеет GUI и управляется через командную строку.

**Запуск:**
```bash
confocal-cli --input image.tif --output result.tif